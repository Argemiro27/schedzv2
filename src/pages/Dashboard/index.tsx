import React from 'react';
import { Button, Container, Text } from '../../styles/Container';
import { useNavigation } from '@react-navigation/native';

export default function Dashboard(){

    return(
        <Container>
            <Text>Seja bem vindo ao Schedz v2.0!</Text>
        </Container>
    )
}